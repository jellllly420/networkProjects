# AutoCut & AutoFlip
```
Group Member: wyZ, zjZ, yfC, ypL
```
## Setup
```bash
chmod +x setup.sh
./setup.sh
```
## Usage
```bash
chmod + x run.sh
./run.sh ${name of video without suffix} ${aspect_ratio} 
```
